# CataForge 升级回滚快照 — 仅本地，通过 `cataforge upgrade rollback --from <ts>` 恢复
.cataforge/.backups/

# RocksDB KG store 运行时日志/锁文件(非数据，每次命令轮转）。kg-first 策略下结构文件(CURRENT/MANIFEST/OPTIONS/*.sst/WAL)是真相源仍入库
.cataforge/kg/store/LOG
.cataforge/kg/store/LOG.old.*
.cataforge/kg/store/LOCK
